{
  "name": "changelog-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic Jira -> GitBook changelog pipeline. No LLM, no external deps.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "selftest": "node scripts/_selftest.js",
    "collect": "node scripts/collect.js",
    "render": "node scripts/render.js",
    "publish": "node scripts/publish.js",
    "build": "node scripts/collect.js && node scripts/render.js",
    "all": "node scripts/collect.js && node scripts/render.js && node scripts/publish.js"
  }
}
