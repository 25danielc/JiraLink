# Local env — never commit real credentials
.env

node_modules/

# Pre-existing Salesforce CLI tooling cache (regenerated by the `sf` CLI) —
# not part of the changelog pipeline, kept out of the repo.
.sfdx/
